# Use CPU-only PyTorch wheels (avoids pulling ~3GB of CUDA packages).
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# Core training dependencies.
# executorch 0.4.0 requires torch==2.5.0 and numpy==1.23.2, so pin to match.
torch==2.5.0
numpy==1.23.2
onnx==1.17.0

# Optional: enables `model.pte` export (--export-pte).
executorch==0.4.0
